# ── runtime ──
node_modules/
.omo/
.npm-cache/
*.tgz
*.log
*:Zone.Identifier
*.Zone.Identifier

# ── build output（本仓库对外提供 lib/，不忽略）──
# lib/

# 构建期临时产物：scripts/build-client.mjs 生成后在 finally 里删除，异常时可能残留
chart-ui/client.runtime.gen.cjs

# ── 运行时数据（Capital 会话在 {{cwd}} 下产出，绝不入库）──
capital-data/
capital-analysis/

# ── 本地研发文档 ──
# 发版说明草稿：正文发布时贴到 GitHub Release 页面，仓库只保留 README 摘要与 CHANGELOG
.release-notes-*.md
.dsh/
ths_api.md
# AGENTS.md（研发索引）与 docs/dev/（详情层）是**研发约定**，随仓库发布：新克隆的
# 研发 Agent 一读就有闸门。使用者 Agent 读的是 persona + skill，两者不共用文本。
docs/*
!docs/sample/
!docs/data-collector-capabilities.md
# 接口账本是 scripts/check-dsh-compat.mjs 的操作手册（探针报错会直接指向它），必须入库。
# 注意：git 不允许重新包含"父目录已被排除"的文件，所以要先放行 docs/reference/ 再逐文件挑选。
!docs/reference/
docs/reference/*
!docs/reference/dsh-surface-ledger.md
# docs/dev/ = AGENTS.md 的详情层，整目录都是研发文档（同样要先放行目录再挑文件）。
!docs/dev/
docs/dev/*
!docs/dev/*.md

# smoke 脚本的临时 profile（正常会自清理，异常退出时可能残留）
.tmp-boot/
.tmp-debug/

# ── 一次性复核/复现脚本的暂存目录（含内部安全复核内容，不随仓库发布）──
.review/

# 研发 Agent 本机的工具授权记录（每跑一条命令就追加，因人而异）；
# .qoder/ 下将来要共享的 commands/workflows 不在此列。
.qoder/settings.local.json
