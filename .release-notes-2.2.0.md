2.1.2 以来最大的一次能力扩容：`data_collector` 61 → 69 个 capability、`web_retriever` 新增九个具名来源查询工具、东财请求收敛到一份网络面。有一处工具改名需迁移：`web_retriever_search` → `anysearch_search`（能力与入参不变）。

## Added
- `data_collector` 新增八个 capability（61 → 69，全部公开端点、**无需新增密钥**）：
  - 腾讯公开 HTTP 三个（行情 fallback）：`tencent_quote`（实时行情/估值/涨跌停/ETF 快照）、`tencent_kline`（日周月复权与 1~60 分钟 K 线）、`tencent_ticks`（最近交易日分笔）
  - 东方财富 HTTP 五个（资金与筹码）：`eastmoney_top_buy_sell_market` / `eastmoney_top_buy_sell_ticker`（龙虎榜全市场与单票汇总）、`eastmoney_lockup_expiry`（限售解禁日历）、`eastmoney_sector_rotation`（板块行情排名）、`eastmoney_cashflow_rotation`（板块资金流）
  - 时间契约同步进 `resolve_data_time_range`（`tencent_kline` 的 start/end 仅日/周/月、三个东财日期区间能力）；能力总表由 `npm run docs:capabilities` 重新生成，测试断言「文档 == 实现」，目录体积仍在剪枝阈值 8192 内
- `web_retriever` 九个具名来源查询工具（provider + feature 命名、零 key）：`cls_telegraph`（财联社 7×24 快讯，`md5(sha1(排序 query 串))` 签名本地计算）、`wscn_lives`（华尔街见闻快讯，`channel` + `cursor` 翻页）、`eastmoney_724`（东财 7×24 快讯，与前两条互为备份）、`cninfo_irm`（巨潮互动易问答，深市，两步 POST）、`sseinfo_qa`（上证e互动问答，沪市，公司列表二分定位 uid）、`eastmoney_stock_news`（个股新闻）、`eastmoney_reports`（个股研报列表）、`sina_reports`（研报第二来源，不含评级与目标价）、`ths_eps_forecast`（同花顺机构一致预期 EPS：逐年 机构数/最小/**均值**/最大/行业平均）
- 这类工具与 `search` / `fetch` 是**两个工作面**：检索是"发现候选 → 按 URL 取正文"，查询是"具名来源 + 业务参数 → 确定、有序、可翻页、同参可复现的结果集"；回执带 `provider` / `operation` / `count` / `next_cursor` / `note`，并按来源分别计入 `provider_tally`（"同一 provider 5 次内收敛"的经验法则因此自动覆盖新来源）
- 东财共享网络面（`src/net/eastmoney-client.ts` + `src/net/throttle.ts`）：进程级单例 + 串行最小间隔 350ms；数据面（Hub 的 `eastmoney_*`）与 web_retriever 侧共用同一个节流器——东财按出口 IP 风控，两套独立限流等于没限（⚠️ Hub 的 FIFO 只保证"同一时刻一个请求"，不含最小间隔：串行 ≠ 节流）
- `createHttpRequester`：出口校验（URL 合法性 + DNS 公网 IP 闸门 + 重定向重校验 + 大小/超时上限 + 取消语义）抽成共用实现，九个来源工具与本地回退走同一条路径，**不引** `ctx.web` / `dsh-tool-web`
- `HttpRequest.encoding`：来源实现可声明正文编码（同花顺一致预期页是 GBK 而响应头不带 charset），**不暴露给模型**

## Changed
- `web_retriever_search` → **`anysearch_search`**（46 处引用、11 个文件同步）。`web_retriever_fetch` **不改名**：它是一条 AnySearch→本机直连的回退链，回执 `via` 标明实际来源
- `web_retriever` persona 与 `capital-web-protocol` skill 扩到两个 provider + 九个来源工具，新增「来源查询纪律」（该翻页就翻到没有、空结果是真事实、深沪不可互换、`status` 是事实不是缺口、`note` 必读、单条正文约 1200 字符上限、失败码含义）；persona 保持瘦身，清单与细则进 skill
- `data_junior` 的 `# BASH DISCIPLINE` 补回软约束：只做纯计算兜底（不是取数通道）、不联网、不读文件、不碰 `capital-data`、时间窗仍优先 `resolve_data_time_range`
- `capital-data-protocol` skill：`request_data` 的 `params` 提示新增腾讯 fallback 能力名（`tencent_quote` / `tencent_kline`，不要改写成已有 `quote` / `history`）
- 分层判据写进注释：数据面按**可复用性**切（数值/分页走 Hub，流式文本走 web_retriever），**网络面只有一份**——判据不是 provider

## Fixed
- 本机直连不再改写 JSON 正文：此前数组括号变 `\[ ... \]`（`JSON.parse` 不再成立）、字段名 `content_text` 变 `content\_text`、`<script>` 内容被当标签删除。现在 JSON 按正文判定并原文直通，HTML 照旧转 markdown
- 接受 `json/javascript` 等 script 类 MIME：严格按 `application/json` 判定会把正常返回 JSON 的接口误判成 `UNSUPPORTED_CONTENT_TYPE`
- 上证e互动公司列表改为**按 `<a>` 元素解析**：窗口式正则在真实页面上会跨条目错配（实测把 `600369` 配成上一条的 `uid=65`）——"能跑但错"的映射比报错危险
- 上证e互动时间支持三类写法（`2026年09月24日 09:30` / `昨天 18:16` / `2分钟前`、`1小时前`）：只认绝对写法会让"最新提问"列表整次失败
- uid 缓存不再把扫描到的整页代码灌进缓存（会在一次解析内塞满容量并淘汰正在解析的代码）
- 九个来源工具的调用方取消原样抛 `ABORTED`，不降级成 `ok:false` 失败信封
- 东财研报预测 EPS 恒为 null：上游返回的是**字符串**（`'1.0700000000'`），按 `typeof === 'number'` 判定会静默当成缺失；现按值转换并补 `eps_next_year`
- 新浪研报日期与类型两列写反（表头为 序号/标题/**报告类型**/**发布日期**/机构/研究员），此前产出 `date:"公司"`、`type:"2026-09-01"`
- 新浪研报个股查询缺交易所前缀会拿到**假空页**（HTTP 200、与"没有研报"同形）；实测间隔 0 连续 4 次全部成功，空页的真实原因是缺前缀而非限流。北交所 `920xxx` 必须先于 `9 → sh` 判定
- `eastmoney_stock_news` 按**键是否存在**区分上游风控与"该股确实没有新闻"，不再静默返回空表
- 同花顺一致预期页：UTF-8 解会产生 11,800 个替换字符，现显式声明 GBK；表定位改为认表头（中英兼容）
- 东财请求统一带浏览器特征 UA

从 2.1.2 升级需迁移一处：自定义 prompt / 脚本里的 `web_retriever_search` 改读 `anysearch_search`（能力与入参不变）；其余升级无需改调用方式，新增能力均为公开端点、无需新增密钥。完整变更见 [CHANGELOG.md](https://github.com/v587d/capital-generation/blob/master/CHANGELOG.md)。
