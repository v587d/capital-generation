## Added
- `describe_dataset`：读一份 Dataset 的默认入口，一次调用完成元数据 + 基础 profile + 可选受控查询，多份数据在同一条消息里并发执行；结果超过 7000 码点时返回带完整列名的 `too_large` 小回执，而不是把超长载荷丢给剪枝器截断
- `time_facts` 增 `axis` / `windows[]` / `covered_*_iso`：日历换算由宿主按该列自身观测到的偏移完成，子 Agent 不再自行把日期算成毫秒
- `query_dataset` 的 filter 直接接受 `YYYY-MM-DD` / `YYYY-MM` / `YYYY` / `{ "period": "last_1_month" }`，结果自动补 `*_iso` 可读时间列
- `resolve_data_time_range` 新增 Dataset 形态：传 `dataset_id` + `period`，返回的 `bounds` 可直接填进 filter（原 `capability + period` 形态行为不变）

## Changed
- `data_junior` 的 persona 与两份 skill 改以 `describe_dataset` 为默认入口，`inspect_dataset` / `profile_dataset` / `query_dataset` 降为单点复核
- 可视化协议补两条硬规则：`chart_source_ref` 一张图一个 token（15 分钟 TTL，不为同一 Dataset 重签）；spec 由 `visualization_specialist` 组装，`data_junior` 只交视图意图

## Fixed
- `resolve_data_time_range` 的 capability 形态每次调用都失败（`output.schema` 的 `required` 校验）、Dataset 形态报 "requires an authorized session"
- `describe_dataset` 内嵌 queries 的时间筛选与 `query_dataset` 行为不一致（归一实现收敛到 `query-time.ts`，两个入口共用）
- `columns_of_interest` 不收窄 `time_facts` 首末值投影；`period` 被序列化成 JSON 字符串时解析不了
- 日期串落到数值时间列被静默跳过：现按 `query_type_conflict` 报错并提示改用 `>=` / `<=`
- `last_N_week` 空窗口退化成 0 天；秒级 epoch 或混合类型时间列被误判成"日粒度毫秒"

非破坏性新增，从 2.1.1 升级无需迁移（两处结果收紧的细节见 [CHANGELOG.md](https://github.com/v587d/capital-generation/blob/master/CHANGELOG.md)）。
